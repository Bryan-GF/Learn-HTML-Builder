import { 

} from '../actions';

const initialState = {
    
};


export const builderReducer = (state = initialState, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
  };
